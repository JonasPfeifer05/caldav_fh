services:
  caldavcalendar:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3102:3000"

    environment:
      NODE_ENV: production
      URL: ${URL}
      USER: ${CIS_USER}
      PASSWORD: ${CIS_PASSWORD}

    restart: always
