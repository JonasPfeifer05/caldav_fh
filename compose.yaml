services:
  caldavcalendar:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3007:3000"

    environment:
      NODE_ENV: production
      URL: ${URL}
      USER: ${USER}
      PASSWORD: ${PASSWORD}

    restart: always
